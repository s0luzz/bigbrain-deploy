import express from 'express';
// import your existing routers/middleware and register them here

const app = express();

// your existing middleware/routes go here
// e.g. app.use(express.json()); app.use('/admin', adminRouter); etc.

export default app;
